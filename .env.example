# Banco de dados (Postgres local via docker-compose por padrão)
DATABASE_URL="postgresql://mapa_astral:mapa_astral@localhost:5432/mapa_astral"

# NextAuth — gere um segredo com: openssl rand -base64 32
AUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Stripe (modo teste) — pegue em https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
NEXT_PUBLIC_STRIPE_PRICE_ESSENCIAL=""
NEXT_PUBLIC_STRIPE_PRICE_MISTICO=""

NEXT_PUBLIC_APP_URL="http://localhost:3000"
